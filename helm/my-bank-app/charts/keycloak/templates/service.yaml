apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.fullnameOverride }}
spec:
  type: NodePort
  selector:
    app: {{ .Values.fullnameOverride }}
  ports:
    - port: 8180
      targetPort: 8180
      nodePort: {{ .Values.service.nodePort }}
