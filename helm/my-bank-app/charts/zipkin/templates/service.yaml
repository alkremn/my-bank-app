apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.fullnameOverride }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.fullnameOverride }}
  ports:
    - port: 9411
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
